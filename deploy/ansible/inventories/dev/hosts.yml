---
# Local development inventory.
#
# The dev environment normally runs via docker-compose.yml at the repo
# root, not via Ansible. This inventory exists so commands can be dry-run
# against a canonical target during playbook development.
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"

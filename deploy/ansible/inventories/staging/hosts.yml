---
# Staging inventory — placeholder.
#
# Hosts will be populated in LLMS-003+ once staging infrastructure is
# provisioned. Until then, `ansible-playbook --check` against this file
# will report no hosts and is the expected state.
all:
  hosts: {}
